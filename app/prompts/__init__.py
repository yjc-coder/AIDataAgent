# Reserved for prompt loaders (Phase 3+).

This package mirrors the Java project's `resources/prompts/` separation:
keeping prompts as plain-text templates instead of inline strings makes
them diff-friendly, reviewable, and A/B-testable by non-coders.
